import { createSlice } from "@reduxjs/toolkit";

const initialState = [];

const dietsSlice = createSlice({
  name: "diets",
  initialState,
  reducers: {},
});

export const {} = dietsSlice.actions;
export default dietsSlice.reducer;
